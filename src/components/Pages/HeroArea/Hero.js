import React from "react";
import SubHeader from "../../Subheader/SubHeader";

const Hero = () => {
  return (
    <section className="hero-area">
      <SubHeader />
    </section>
  );
};

export default Hero;
